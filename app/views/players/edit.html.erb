<h1>Editing player</h1>

<% form_for(@player) do |f| %>
  <%= f.error_messages %>
	<%= render :partial => 'form' %>
  <p>
    <%= f.submit 'Update' %>
  </p>
<% end %>

<%= link_to 'Show', @player %> |
<%= link_to 'Back', players_path %>